[build]
dockerfilePath = "Dockerfile.web"

[deploy]
startCommand = "uvicorn tateyomi.web.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
